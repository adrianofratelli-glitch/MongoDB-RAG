-r requirements.txt

# One-shot document ingestion only; deliberately absent from the API image.
langchain-text-splitters>=1.0,<2
pypdf>=6.0,<7
docx2txt>=0.8,<1
beautifulsoup4>=4.12,<5
openpyxl>=3.1,<4
python-pptx>=1.0,<2

# Document loaders used by ingest.py (PDF/DOCX/TXT/CSV/HTML) and bench/corpus_stats.py.
langchain-community>=0.4,<0.5
